import React from 'react';
import Form from './Form';

const OrginalAuthentication = () => {
    return (
        <div>
            <Form/>
        </div>
    );
};

export default OrginalAuthentication;